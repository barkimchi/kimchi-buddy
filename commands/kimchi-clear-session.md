---
description: Reset the session timer
allowed-tools: [Bash]
---

Run this command to reset the session timer:

```bash
bash ~/.claude/kimchi/widgets.sh session-reset
```

Display the output. Do not add extra commentary.
