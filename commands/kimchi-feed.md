---
description: Feed Kimchi a snack
allowed-tools: [Bash]
---

Run this command and display the output exactly as-is (it contains pre-formatted ASCII art in a code block):

```bash
bash ~/.claude/kimchi/kimchi.sh feed
```

Do not add any extra commentary. Just show Kimchi's reaction.
