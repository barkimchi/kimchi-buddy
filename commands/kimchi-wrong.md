---
description: Report a Kimchi rendering issue
argument-hint: <description of what looks wrong>
allowed-tools: [Bash]
---

Run this command to log the rendering issue:

```bash
bash ~/.claude/kimchi/kimchi.sh wrong "$ARGUMENTS"
```

Then display: "Got it, logged the issue. I'll try to do better."
