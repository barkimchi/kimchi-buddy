---
description: Check on Kimchi's status
allowed-tools: [Bash]
---

Run this command and display the output exactly as-is (it contains a pre-formatted status view in a code block):

```bash
bash ~/.claude/kimchi/kimchi.sh buddy
```

Do not add any extra commentary. Just show the status view.
